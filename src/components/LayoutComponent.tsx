
import React,{FC} from "react";

interface LayoutComponetProps {
    children: React.ReactNode;
   
  }

  const LayoutComponent:FC<LayoutComponetProps >=()=>{
return(
    <div>
        <h1>pppp</h1>
    </div>
)
  }
export default LayoutComponent